<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.mbs.mapper.MemberMapper">
  
  	<insert id="join" parameterType="com.mbs.dto.MemberDto">
		Insert into tbl_test(USERID, USERPASS, USERNAME, USERPNUM) 
		values(#{userId}, #{userPass}, #{userName}, #{userPnum})
	</insert>
	
  </mapper>